<!DOCTYPE html>
<html>
  <head>
    <title>Contact Us</title>
    <style>
      .form-group {
        margin-bottom: 10px;
      }

      .form-group label {
        font-weight: bold;
        margin-bottom: 5px;
      }

      .form-group input,
      .form-group textarea {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 4px;
        resize: none;
      }

      .form-group textarea {
        height: 100px;
      }
    </style>
  </head>

  <body>
    <h2>Contact Us</h2>

    <div className="form-group">
      <label for="name">Name:</label>
      <span> <%= flightDetails.contactForm.name %> </span>
    </div>

    <div className="form-group">
      <label for="email">Email:</label>
      <span> <%= flightDetails.contactForm.email %> </span>
    </div>

    <div className="form-group">
      <label for="phone">Phone:</label>
      <span> <%= flightDetails.contactForm.phone %> </span>
    </div>

    <div className="form-group">
      <label for="subject">Subject:</label>
      <span> <%= flightDetails.contactForm.subject %> </span>
    </div>

    <div className="form-group">
      <label for="message">Message:</label>
      <span> <%= flightDetails.contactForm.message %> </span>
    </div>
  </body>
</html>
